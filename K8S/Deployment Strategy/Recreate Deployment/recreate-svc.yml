apiVersion: v1
kind: Service
metadata:
  name: recreate-service
  namespace: recreate-ns
spec:
  selector:
    app: top-course
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000
